# Host port published by Docker Compose.
PORT=8000

# Printer registrations are persisted here.
PRINTER_REGISTRY_PATH=data/printers.json

# SNMP v1 defaults. Change these to match the printer configuration.
PRINTER_SNMP_READ_COMMUNITY=public
PRINTER_SNMP_WRITE_COMMUNITY=private
